# Transcribe Tools

Stage-owned deterministic helpers for transcription planning and project
scaffolding live here.

Run scripts through the repo venv from the repository root:

```powershell
.\.venv\Scripts\python.exe transcribe\tools\<script>.py
```

Use `texgraph` CLI commands first when they exist. Fall back to these scripts
only for operations the CLI does not cover.
