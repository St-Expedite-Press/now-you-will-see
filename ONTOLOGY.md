# ONTOLOGY.md

The canonical ontology lives at `machinery/docs/ONTOLOGY.md`.

Read `AGENTS.md` first, then use `machinery/docs/ONTOLOGY.md` for directory
taxonomy, schemas, commands, invariants, and update protocol.
