# Texgraph — consolidated dependency manifest
# Install the editable package (includes all core deps from pyproject.toml):
#
#   pip install -e .
#
# For Studio (FastAPI backend + Anthropic agent service), use the optional
# dependency group documented in machinery/docs/STUDIO_FRONTEND.md:
#
#   pip install -e ".[studio]"
#
# This file lists everything explicitly for environments that require a flat
# requirements file instead of pyproject.toml extras.

# ── Core build and editorial tools ─────────────────────────────────────────
python-frontmatter>=1.1.0
mistune>=3.0.2
jinja2>=3.1.4
pyyaml>=6.0.2
typer>=0.12.5
rich>=13.7.1
watchdog>=4.0.1
python-dotenv>=1.0.1
pydantic>=2.0

# ── Studio (FastAPI backend + agent service) ────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic-settings>=2.2.0
aiofiles>=23.2.1
python-multipart>=0.0.9
watchfiles>=0.21.0
anthropic>=0.28.0

# ── Development ─────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.4.0
mypy>=1.10.0
