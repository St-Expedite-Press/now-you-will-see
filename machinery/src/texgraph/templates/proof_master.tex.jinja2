% Texgraph proof master — generated by texgraph proof-build
% Do not edit directly. All content is in the fragment files under tex/.
\BLOCK{set rc = config.render_config or {}}
%# openany: chapter-level matter (introduction, per-volume notes, afterword)
%# opens on the next page, not a forced recto — avoids blank versos carrying a
%# stray folio.  Book title pages still force a recto via \cleardoublepage.
\documentclass[\VAR{rc.get("fontsize") or "11pt"}, openany, twoside]{book}
\input{preamble}

\begin{document}

%# ── Front matter ─────────────────────────────────────────────────────────────
\frontmatter
\pagestyle{empty}
\BLOCK{for path in frontmatter_paths}
\input{\VAR{path}}
\BLOCK{endfor}

%# ── Main matter ──────────────────────────────────────────────────────────────
\mainmatter
\pagestyle{fancy}
\BLOCK{for sec in main_sections}
\setcounter{endnote}{0}%
\BLOCK{for path in sec.paths}
\input{\VAR{path}}
\BLOCK{endfor}
\BLOCK{if sec.print_endnotes}
\renewcommand{\notesname}{Notes to \VAR{sec.title}}
\theendnotes
\BLOCK{endif}
\BLOCK{endfor}

%# ── Back matter ──────────────────────────────────────────────────────────────
\backmatter
\pagestyle{fancy}
\BLOCK{for path in backmatter_paths}
\input{\VAR{path}}
\BLOCK{endfor}

\end{document}
