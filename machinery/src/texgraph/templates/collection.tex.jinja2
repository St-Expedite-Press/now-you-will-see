% Texgraph — generated LuaLaTeX document
% Do not edit this file directly; edit the Markdown sources and re-run `texgraph build`.
\BLOCK{set rc = config.render_config or {}}
\documentclass[\VAR{rc.get("fontsize") or "11pt"}, openright, twoside]{book}

%# ── Engine & font setup ────────────────────────────────────────────────────
\usepackage{fontspec}
\usepackage{microtype}
\setmainfont{\VAR{rc.get("mainfont") or "EB Garamond"}}[
  Ligatures = TeX,
  Numbers   = OldStyle,
]

%# ── Language ───────────────────────────────────────────────────────────────
\usepackage{polyglossia}
\setdefaultlanguage{\VAR{config.language}}

%# ── Page geometry ──────────────────────────────────────────────────────────
\usepackage[
  paperwidth=\VAR{rc.get("paperwidth") or "6in"},
  paperheight=\VAR{rc.get("paperheight") or "9in"},
  top=\VAR{rc.get("top_margin") or "0.875in"},
  bottom=\VAR{rc.get("bottom_margin") or "0.875in"},
  inner=\VAR{rc.get("inner_margin") or "1in"},
  outer=\VAR{rc.get("outer_margin") or "0.75in"},
  includeheadfoot,
]{geometry}

%# ── Poetry environment ─────────────────────────────────────────────────────
\usepackage{verse}

%# ── Running heads ──────────────────────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyhead[LE]{\small\scshape\MakeLowercase{\VAR{config.author}}}
\fancyhead[RO]{\small\itshape\nouppercase{\rightmark}}
\fancyfoot[LE,RO]{\small\thepage}
\fancypagestyle{plain}{%
  \fancyhf{}%
  \renewcommand{\headrulewidth}{0pt}%
  \fancyfoot[LE,RO]{\small\thepage}%
}

%# ── Hyperref / PDF metadata ────────────────────────────────────────────────
\usepackage[
  pdftitle={\VAR{config.title}},
  pdfauthor={\VAR{config.author}},
  hidelinks,
]{hyperref}

%# ── PDF/X (production only) ────────────────────────────────────────────────
\BLOCK{if not config.draft_mode}
\usepackage{pdfx}
\BLOCK{endif}

%# ── Spacing & utilities ────────────────────────────────────────────────────
\usepackage{setspace}
\BLOCK{if rc.get("line_spread")}
\linespread{\VAR{rc.get("line_spread")}}\selectfont
\BLOCK{endif}
\usepackage{emptypage}

%# ── Custom commands ────────────────────────────────────────────────────────

%# Poem title: redefine verse package's \poemtitle with our preferred styling
\renewcommand{\poemtitle}[1]{%
  \vspace{1.5\baselineskip}%
  {\noindent\normalfont\itshape\large #1\par}%
  \vspace{0.5\baselineskip}%
  \noindent
}

%# Part heading within a section: small caps, centered, with space around it
\newcommand{\partheading}[1]{%
  \vspace{2\baselineskip}%
  \begin{center}%
    {\normalfont\footnotesize\scshape\MakeLowercase{#1}}%
  \end{center}%
  \vspace{0.75\baselineskip}%
  \nopagebreak[4]%
}

% ============================================================================
\begin{document}

%# ════════════════════════════════════════════════════════════════════════════
%# FRONT MATTER
%# ════════════════════════════════════════════════════════════════════════════
\frontmatter
\pagestyle{empty}

\BLOCK{for section in sections}
\BLOCK{if section.meta.get("type") == "frontmatter"}
\BLOCK{for poem in section.poems}

%# ── Half-title page ─────────────────────────────────────────────────────────
\BLOCK{if poem.type == "halftitle"}
\clearpage
\thispagestyle{empty}
\vspace*{\fill}
\begin{center}
  {\normalfont\Large\itshape \VAR{config.title}\par}
\end{center}
\vspace*{\fill}
\clearpage

%# ── Title page ───────────────────────────────────────────────────────────────
\BLOCK{elif poem.type == "titlepage"}
\clearpage
\thispagestyle{empty}
\vspace*{2in}
\begin{center}
  {\normalfont\huge\itshape \VAR{config.title}\par}
  \BLOCK{if config.subtitle}
  \vspace{1.5ex}
  {\normalfont\normalsize \VAR{config.subtitle}\par}
  \BLOCK{endif}
  \vspace{2in}
  {\normalfont\normalsize\scshape\MakeLowercase{\VAR{config.author}}\par}
  \BLOCK{if config.publisher}
  \vfill
  {\normalfont\small \VAR{config.publisher}\par}
  \BLOCK{endif}
  {\normalfont\small \VAR{config.year}\par}
\end{center}
\clearpage

%# ── Copyright page ───────────────────────────────────────────────────────────
\BLOCK{elif poem.type == "copyright"}
\clearpage
\thispagestyle{empty}
\vspace*{\fill}
{\small\setlength{\parskip}{0.75\baselineskip}\setlength{\parindent}{0pt}%
\BLOCK{for stanza in poem.stanzas}
\noindent
\BLOCK{for line in stanza.lines}
\VAR{line}
\BLOCK{endfor}
\par
\BLOCK{if not loop.last}
\medskip
\BLOCK{endif}
\BLOCK{endfor}
}%
\clearpage

%# ── Table of contents ────────────────────────────────────────────────────────
\BLOCK{elif poem.type == "toc"}
\clearpage
\pagestyle{plain}
\tableofcontents
\clearpage
\pagestyle{empty}

%# ── Introduction / preface / foreword ────────────────────────────────────────
\BLOCK{elif poem.type in ["introduction", "preface", "foreword", "prose"]}
\clearpage
\pagestyle{plain}
\chapter*{\VAR{poem.title}}
\addcontentsline{toc}{chapter}{\VAR{poem.title}}
\BLOCK{for stanza in poem.stanzas}
\BLOCK{for line in stanza}
\VAR{line}\par
\BLOCK{endfor}
\medskip
\BLOCK{endfor}
\clearpage
\pagestyle{empty}

\BLOCK{endif}
\BLOCK{endfor}
\BLOCK{endif}
\BLOCK{endfor}

%# ════════════════════════════════════════════════════════════════════════════
%# MAIN MATTER
%# ════════════════════════════════════════════════════════════════════════════
\mainmatter
\pagestyle{fancy}

\BLOCK{for section in sections}
\BLOCK{if section.meta.get("type") not in ["frontmatter", "backmatter"]}
\BLOCK{set ns = namespace(prev_part="")}

\BLOCK{for poem in section.poems}

%# ── Section / book title page ────────────────────────────────────────────────
\BLOCK{if poem.type == "section-title"}
\clearpage
\thispagestyle{empty}
\vspace*{\fill}
\begin{center}
  {\normalfont\LARGE\itshape \VAR{section.meta.get("label") or section.title}\par}
  \BLOCK{if poem.meta.get("subtitle")}
  \vspace{1.5ex}
  {\normalfont\small \VAR{poem.meta.get("subtitle")}\par}
  \BLOCK{endif}
  \BLOCK{if poem.meta.get("note")}
  \vspace{4ex}
  {\normalfont\footnotesize\itshape \VAR{poem.meta.get("note")}\par}
  \BLOCK{endif}
\end{center}
\vspace*{\fill}
\clearpage
\markright{\VAR{section.meta.get("label") or section.title}}

%# ── Dedicatory poem (full poem, centered on its own page) ───────────────────
\BLOCK{elif poem.type == "dedicatory-poem"}
\clearpage
\thispagestyle{empty}
\vspace*{\fill}
\begin{center}
{\small\itshape \VAR{poem.title}\par}
\vspace{2ex}
\BLOCK{for stanza in poem.stanzas}
\begin{verse}
\BLOCK{for line in stanza.lines}
\VAR{line}
\BLOCK{endfor}
\end{verse}
\BLOCK{if not loop.last}
\vspace{\VAR{rc.get("stanza_skip") or "1.2ex"}}
\BLOCK{endif}
\BLOCK{endfor}
\end{center}
\vspace*{\fill}
\clearpage

%# ── Dedication page (prose or list, centered) ────────────────────────────────
\BLOCK{elif poem.type == "dedication"}
\clearpage
\thispagestyle{empty}
\vspace*{\fill}
\begin{center}
{\small\itshape \VAR{poem.title}\par}
\vspace{2ex}
{\small\setlength{\parskip}{0.4\baselineskip}\setlength{\parindent}{0pt}%
\BLOCK{for stanza in poem.stanzas}
\BLOCK{for line in stanza.lines}
\VAR{line}\par
\BLOCK{endfor}
\BLOCK{if not loop.last}
\medskip
\BLOCK{endif}
\BLOCK{endfor}
}%
\end{center}
\vspace*{\fill}
\clearpage

%# ── Prose piece within a section ─────────────────────────────────────────────
\BLOCK{elif poem.type == "prose"}
\section*{\VAR{poem.title}}
\BLOCK{for stanza in poem.stanzas}
\BLOCK{for line in stanza}
\VAR{line}\par
\BLOCK{endfor}
\medskip
\BLOCK{endfor}

%# ── Regular poem (and poem-cycle and screenplay) ─────────────────────────────
\BLOCK{else}

%# Insert part heading when part changes
\BLOCK{if poem.meta.get("part", "") and poem.meta.get("part", "") != ns.prev_part}
\partheading{\VAR{poem.meta.get("part", "")}}
\BLOCK{set ns.prev_part = poem.meta.get("part", "")}
\BLOCK{endif}

\poemtitle{\VAR{poem.title}}
\BLOCK{if poem.epigraph}
\begin{quote}\itshape
\VAR{poem.epigraph}
\BLOCK{if poem.epigraph_author}
\par\smallskip\hfill\upshape\small \VAR{poem.epigraph_author}
\BLOCK{endif}
\end{quote}
\BLOCK{endif}
\BLOCK{if poem.dedication}
\noindent\textit{\VAR{poem.dedication}}\par\medskip
\BLOCK{endif}

\BLOCK{if poem.is_cycle}
%# ── Poem cycle
\BLOCK{for part in poem.parts}
\BLOCK{if part.title}
{\itshape \VAR{part.title}}\nopagebreak\medskip
\BLOCK{endif}
\BLOCK{for stanza in part.stanzas}
\BLOCK{if stanza.is_blockquote}
\begin{quote}\itshape
\BLOCK{for line in stanza.lines}
\VAR{line}
\BLOCK{endfor}
\end{quote}
\BLOCK{else}
\begin{verse}
\BLOCK{for line in stanza.lines}
\VAR{line}
\BLOCK{endfor}
\end{verse}
\BLOCK{endif}
\BLOCK{if not loop.last}
\vspace{\VAR{rc.get("stanza_skip") or "1.2ex"}}
\BLOCK{endif}
\BLOCK{endfor}
\BLOCK{if not loop.last}
\medskip
\BLOCK{endif}
\BLOCK{endfor}

\BLOCK{elif poem.is_screenplay}
%# ── Poem screenplay
\BLOCK{for scene in poem.scenes}
\BLOCK{if not loop.first}
\medskip\noindent\rule{\linewidth}{0.4pt}\medskip
\BLOCK{endif}
\BLOCK{for stanza in scene.stanzas}
\BLOCK{if stanza.is_blockquote}
\begin{quote}\itshape
\BLOCK{for line in stanza.lines}
\VAR{line}
\BLOCK{endfor}
\end{quote}
\BLOCK{else}
\begin{verse}
\BLOCK{for line in stanza.lines}
\VAR{line}
\BLOCK{endfor}
\end{verse}
\BLOCK{endif}
\BLOCK{if not loop.last}
\vspace{\VAR{rc.get("stanza_skip") or "1.2ex"}}
\BLOCK{endif}
\BLOCK{endfor}
\BLOCK{endfor}

\BLOCK{else}
%# ── Standard verse poem
\BLOCK{for stanza in poem.stanzas}
\BLOCK{if stanza.is_blockquote}
\begin{quote}\itshape
\BLOCK{for line in stanza.lines}
\VAR{line}
\BLOCK{endfor}
\end{quote}
\BLOCK{else}
\begin{verse}
\BLOCK{for line in stanza.lines}
\VAR{line}
\BLOCK{endfor}
\end{verse}
\BLOCK{endif}
\BLOCK{if not loop.last}
\vspace{\VAR{rc.get("stanza_skip") or "1.2ex"}}
\BLOCK{endif}
\BLOCK{endfor}
\BLOCK{endif}

\BLOCK{endif}%# end poem type
\BLOCK{endfor}%# end poems in section
\BLOCK{endif}%# end non-frontmatter/backmatter
\BLOCK{endfor}%# end sections (mainmatter)

%# ════════════════════════════════════════════════════════════════════════════
%# BACK MATTER
%# ════════════════════════════════════════════════════════════════════════════
\backmatter

\BLOCK{for section in sections}
\BLOCK{if section.meta.get("type") == "backmatter"}
\BLOCK{for poem in section.poems}
\clearpage
\pagestyle{plain}
\chapter*{\VAR{poem.title}}
\addcontentsline{toc}{chapter}{\VAR{poem.title}}
\BLOCK{if poem.type == "prose"}
\BLOCK{for stanza in poem.stanzas}
\BLOCK{for line in stanza}
\VAR{line}\par
\BLOCK{endfor}
\medskip
\BLOCK{endfor}
\BLOCK{else}
\BLOCK{for stanza in poem.stanzas}
\BLOCK{for line in stanza.lines}
\VAR{line}\par
\BLOCK{endfor}
\medskip
\BLOCK{endfor}
\BLOCK{endif}
\clearpage
\pagestyle{fancy}
\BLOCK{endfor}
\BLOCK{endif}
\BLOCK{endfor}

\end{document}
